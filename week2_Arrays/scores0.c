#include <stdio.h>

int main(void)
{
    // Simplest versio of the scores program
    int score1 = 72;
    int score2 = 73;
    int score3 = 74;

    printf("Average: %f\n", (score1 + score2 + score3) / 3.0);
}